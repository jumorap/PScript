printm("Simple else if");

a = true;
printm("The value of 'a' is:");
print(a);
if (a) {
    printm(a_is_true);
} else {
    printm(a_is_false);
};


a = false;
printm("The value of 'a' is:");
print(a);
if (a) {
    printm(a_is_true);
} else {
    printm(a_is_false);
};
