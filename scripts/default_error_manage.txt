SEED = 1;
MU = 5;
SIGMA = 1;
LAMBDA = 80;
SUCCESS = 1.5;

a = [1, 2, 3, 4];

printm("SUCCESS:");
print(SUCCESS);
printm("SUCCESS has a value > 1, then we'll get an error:");
a[1] = GeoRand;
printm("SUCCESS:");
print(SUCCESS);
